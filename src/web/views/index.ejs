<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #0f0f23 0%, #1a1a3e 50%, #0f0f23 100%);
            color: #ffffff;
            min-height: 100vh;
            overflow-x: hidden;
        }

        /* Animated background */
        .bg-animation {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            opacity: 0.1;
        }

        .bg-animation::before {
            content: '';
            position: absolute;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, #5865f2 1px, transparent 1px);
            background-size: 50px 50px;
            animation: move 20s linear infinite;
        }

        @keyframes move {
            0% { transform: translate(0, 0); }
            100% { transform: translate(-50px, -50px); }
        }

        /* Navbar */
        .navbar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1rem 5%;
            background: rgba(0, 0, 0, 0.4);
            backdrop-filter: blur(10px);
            position: fixed;
            width: 100%;
            top: 0;
            z-index: 1000;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }

        .logo {
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .logo img {
            width: 40px;
            height: 40px;
            border-radius: 50%;
        }

        .logo h1 {
            font-size: 1.5rem;
            background: linear-gradient(90deg, #5865f2, #eb459e);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .nav-links {
            display: flex;
            list-style: none;
            gap: 2rem;
            align-items: center;
        }

        .nav-links a {
            color: #b9bbbe;
            text-decoration: none;
            transition: color 0.3s;
            font-weight: 500;
        }

        .nav-links a:hover {
            color: #fff;
        }

        .btn-nav {
            background: linear-gradient(90deg, #5865f2, #4752c4);
            padding: 0.6rem 1.5rem;
            border-radius: 25px;
            color: white !important;
            font-weight: 600;
            transition: transform 0.3s, box-shadow 0.3s;
        }

        .btn-nav:hover {
            transform: scale(1.05);
            box-shadow: 0 5px 20px rgba(88, 101, 242, 0.4);
        }

        /* Hero Section */
        .hero {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            padding: 2rem;
            padding-top: 100px;
        }

        .hero-content {
            max-width: 800px;
        }

        .hero-content .icon {
            font-size: 5rem;
            margin-bottom: 1rem;
            animation: bounce 2s ease-in-out infinite;
        }

        @keyframes bounce {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-15px); }
        }

        .hero-content h1 {
            font-size: 3.5rem;
            margin-bottom: 1rem;
            background: linear-gradient(90deg, #fff, #5865f2, #eb459e);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .hero-content p {
            font-size: 1.3rem;
            color: #b9bbbe;
            margin-bottom: 2rem;
        }

        /* Stats Cards */
        .stats-container {
            display: flex;
            justify-content: center;
            gap: 1.5rem;
            margin: 2.5rem 0;
            flex-wrap: wrap;
        }

        .stat-card {
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.1);
            padding: 1.5rem 2.5rem;
            border-radius: 15px;
            backdrop-filter: blur(10px);
            transition: transform 0.3s, border-color 0.3s;
            min-width: 150px;
        }

        .stat-card:hover {
            transform: translateY(-5px);
            border-color: #5865f2;
        }

        .stat-card h3 {
            font-size: 2.5rem;
            background: linear-gradient(90deg, #5865f2, #eb459e);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .stat-card p {
            color: #b9bbbe;
            font-size: 0.9rem;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        /* Buttons */
        .buttons {
            display: flex;
            gap: 1rem;
            justify-content: center;
            flex-wrap: wrap;
            margin-top: 2rem;
        }

        .btn {
            padding: 1rem 2.5rem;
            border-radius: 30px;
            text-decoration: none;
            font-weight: 600;
            font-size: 1rem;
            transition: all 0.3s;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
        }

        .btn-primary {
            background: linear-gradient(90deg, #5865f2, #4752c4);
            color: white;
            box-shadow: 0 5px 20px rgba(88, 101, 242, 0.3);
        }

        .btn-primary:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 30px rgba(88, 101, 242, 0.5);
        }

        .btn-secondary {
            background: transparent;
            border: 2px solid rgba(255, 255, 255, 0.3);
            color: white;
        }

        .btn-secondary:hover {
            border-color: #5865f2;
            background: rgba(88, 101, 242, 0.1);
        }

        /* Features Section */
        .features {
            padding: 5rem 10%;
            background: rgba(0, 0, 0, 0.2);
        }

        .features h2 {
            text-align: center;
            font-size: 2.5rem;
            margin-bottom: 3rem;
            background: linear-gradient(90deg, #fff, #5865f2);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .feature-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 2rem;
            max-width: 1200px;
            margin: 0 auto;
        }

        .feature-card {
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid rgba(255, 255, 255, 0.08);
            padding: 2rem;
            border-radius: 20px;
            text-align: center;
            transition: all 0.3s;
        }

        .feature-card:hover {
            transform: translateY(-10px);
            border-color: #5865f2;
            background: rgba(88, 101, 242, 0.05);
        }

        .feature-card .icon {
            font-size: 3rem;
            margin-bottom: 1rem;
        }

        .feature-card h3 {
            margin-bottom: 0.5rem;
            font-size: 1.3rem;
        }

        .feature-card p {
            color: #b9bbbe;
            font-size: 0.95rem;
        }

        /* Footer */
        footer {
            text-align: center;
            padding: 2rem;
            background: rgba(0, 0, 0, 0.4);
            color: #72767d;
            border-top: 1px solid rgba(255, 255, 255, 0.05);
        }

        footer a {
            color: #5865f2;
            text-decoration: none;
        }

        /* Status indicator */
        .status {
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            background: rgba(87, 242, 135, 0.1);
            padding: 0.3rem 1rem;
            border-radius: 20px;
            margin-bottom: 1rem;
        }

        .status-dot {
            width: 10px;
            height: 10px;
            background: #57f287;
            border-radius: 50%;
            animation: pulse 2s ease-in-out infinite;
        }

        @keyframes pulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.5; }
        }

        /* Responsive */
        @media (max-width: 768px) {
            .nav-links {
                display: none;
            }
            
            .hero-content h1 {
                font-size: 2rem;
            }

            .hero-content p {
                font-size: 1rem;
            }
            
            .stat-card {
                padding: 1rem 1.5rem;
            }

            .stat-card h3 {
                font-size: 1.8rem;
            }

            .btn {
                padding: 0.8rem 1.5rem;
                font-size: 0.9rem;
            }
        }
    </style>
</head>
<body>
    <div class="bg-animation"></div>

    <nav class="navbar">
        <div class="logo">
            <% if (botAvatar) { %>
                <img src="<%= botAvatar %>" alt="<%= botName %>">
            <% } %>
            <h1><%= botName %></h1>
        </div>
        <ul class="nav-links">
            <li><a href="/">Home</a></li>
            <li><a href="/api/stats">Stats</a></li>
            <li><a href="/support">Support</a></li>
            <li><a href="/invite" class="btn-nav"><i class="fas fa-plus"></i> Add Bot</a></li>
        </ul>
    </nav>

    <main class="hero">
        <div class="hero-content">
            <div class="status">
                <span class="status-dot"></span>
                <span>Online • <%= ping %>ms</span>
            </div>
            
            <div class="icon">🎵</div>
            <h1><%= botName %></h1>
            <p>High-quality music streaming for your Discord server with advanced features, filters, and seamless performance.</p>
            
            <div class="stats-container">
                <div class="stat-card">
                    <h3><%= serverCount.toLocaleString() %></h3>
                    <p>Servers</p>
                </div>
                <div class="stat-card">
                    <h3><%= userCount.toLocaleString() %></h3>
                    <p>Users</p>
                </div>
                <div class="stat-card">
                    <h3>24/7</h3>
                    <p>Uptime</p>
                </div>
                <div class="stat-card">
                    <h3>50+</h3>
                    <p>Commands</p>
                </div>
            </div>

            <div class="buttons">
                <a href="/invite" class="btn btn-primary">
                    <i class="fab fa-discord"></i> Add to Discord
                </a>
                <a href="/support" class="btn btn-secondary">
                    <i class="fas fa-headset"></i> Support Server
                </a>
            </div>
        </div>
    </main>

    <section class="features">
        <h2>✨ Features</h2>
        <div class="feature-grid">
            <div class="feature-card">
                <div class="icon">🎶</div>
                <h3>Multi-Platform</h3>
                <p>Stream from YouTube, Spotify, SoundCloud, and more sources.</p>
            </div>
            <div class="feature-card">
                <div class="icon">🎛️</div>
                <h3>Audio Filters</h3>
                <p>Bassboost, Nightcore, Vaporwave, 8D Audio, and many more!</p>
            </div>
            <div class="feature-card">
                <div class="icon">📋</div>
                <h3>Queue System</h3>
                <p>Advanced queue with shuffle, loop, skip, and jump features.</p>
            </div>
            <div class="feature-card">
                <div class="icon">🔊</div>
                <h3>24/7 Mode</h3>
                <p>Keep the music playing non-stop in your server.</p>
            </div>
            <div class="feature-card">
                <div class="icon">📝</div>
                <h3>Lyrics</h3>
                <p>Get synchronized lyrics for your favorite songs.</p>
            </div>
            <div class="feature-card">
                <div class="icon">⚡</div>
                <h3>Low Latency</h3>
                <p>Optimized for the best audio quality and performance.</p>
            </div>
        </div>
    </section>

    <footer>
        <p>© 2024 <%= botName %> • Made with ❤️ by <a href="/support">Aori Team</a></p>
        <p style="margin-top: 0.5rem; font-size: 0.8rem;">
            <a href="/api/status">API</a> • 
            <a href="/api/stats">Stats</a> • 
            <a href="/health">Health</a>
        </p>
    </footer>
</body>
</html>